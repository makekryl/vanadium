## Vanadium Core Tests

Snapshot-based end-to-end tests are used to test Vanadium Core functionality (frontend's semantic analyzer & type checker integration).

### How to write a test

Tests are written either to ensure that analysis a test suite does *not* emit any error, either that a set of *concrete* errors is emitted during analysis.
