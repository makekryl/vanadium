module NumericTypes {

function fn() {
  var integer i := 0;
  var float f := 0.0;
  //
  var integer i2 := f;
  var float f2 := i;

  i := i + 1;
  f := f + 1.0;
  //
  i := i - 2;
  f := f - 2.0;
  //
  i := i * 3;
  f := f * 3.0;
  //
  i := i / 3;
  f := f / 3.0;

  //

  i := i + 1.0;
  f := f + 1;
  //
  i := i - 2.0;
  f := f - 2;
  //
  i := i * 3.0;
  f := f * 3;
  //
  i := i / 3.0;
  f := f / 3;

  //

  i := f;
  f := i;
}

}
